apiVersion: v1
kind: Service
metadata:
  name: modelforge-service
  namespace: default
  labels:
    app: modelforge
spec:
  selector:
    app: modelforge
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  type: ClusterIP   # or LoadBalancer/NodePort for external access